# Getting Git

http://git-scm.com/

!SLIDE commandline

# .gitconfig

    $ git config --global user.name \
            "Chris Peplin"

    $ git config --global user.email \
            "cpeplin@ford.com"

!SLIDE commandline

    $ cat ~/.gitconfig
    [user]
        name = Chris Peplin
        email = cpeplin@ford.com
    [color]
        status = auto
        branch = auto
        interactive = auto
        diff = auto
